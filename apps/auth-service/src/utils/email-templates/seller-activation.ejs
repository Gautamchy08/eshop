<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify Your Email</title>
    <style>
      /* Reset styles */
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        -webkit-text-size-adjust: none;
        text-size-adjust: none;
      }

      /* Container styles */
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: #ffffff;
      }

      /* Header styles */
      .header {
        text-align: center;
        padding: 20px 0;
        background-color: #007bff;
        color: white;
      }

      /* Logo styles */
      .logo {
        max-width: 150px;
        margin-bottom: 20px;
      }

      /* Content styles */
      .content {
        padding: 30px 20px;
        line-height: 1.6;
        color: #333333;
      }

      /* OTP styles */
      .otp-container {
        text-align: center;
        margin: 30px 0;
        padding: 20px;
        background-color: #f8f9fa;
        border-radius: 8px;
      }

      .otp-code {
        font-size: 32px;
        font-weight: bold;
        letter-spacing: 5px;
        color: #007bff;
        margin: 10px 0;
      }

      /* Warning styles */
      .warning {
        font-size: 14px;
        color: #dc3545;
        margin-top: 20px;
        text-align: center;
      }

      /* Footer styles */
      .footer {
        text-align: center;
        padding: 20px;
        color: #666666;
        font-size: 12px;
        background-color: #f8f9fa;
      }

      /* Button styles */
      .button {
        display: inline-block;
        padding: 12px 30px;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin-top: 20px;
      }

      /* Responsive styles */
      @media only screen and (max-width: 480px) {
        .container {
          width: 100% !important;
          padding: 10px !important;
        }
        .content {
          padding: 15px !important;
        }
        .otp-code {
          font-size: 24px !important;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <!-- Replace with your logo -->
        <h1>eShop</h1>
      </div>

      <div class="content">
        <h2>Verify Your Email Address</h2>
        <p>Hi <%= name %>,</p>
        <p>
          Thank you for registering with eshop! To active your seller account,
          please use the following One-Time Password (OTP):
        </p>

        <div class="otp-container">
          <div class="otp-code"><%= otp %></div>
          <p>This code will expire in <strong>10 minutes</strong></p>
        </div>

        <p>
          If you didn't register  for a eshop seller account, please ignore this email or
          contact our support team if you have concerns.
        </p>

        <div class="warning">
          <p>
            ⚠️ Never share this OTP with anyone, including eShop support team.
          </p>
        </div>
      </div>

      <div class="footer">
        <p>This is an automated message, please do not reply to this email.</p>
        <p>
          &copy; <%= new Date().getFullYear() %> eShop. All rights reserved.
        </p>
        <p>
          Questions? Visit our <a href="mailto:gautamchy8789@gmail.com">Help Center</a> or
          contact <a href="mailto:gautamchy8789@gmail.com">Support</a>
        </p>
      </div>
    </div>
  </body>
</html>
